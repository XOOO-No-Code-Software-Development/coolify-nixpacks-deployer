providers = ["python"]

[phases.setup]
nixPkgs = ["curl", "unzip", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "bash download-source.sh",
  "if [ -f requirements.txt ]; then pip install -r requirements.txt; fi"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8000"
